<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.austlm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/aust_lm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aust_lm"
        android:supportsRtl="true"
        android:theme="@style/Theme.AUSTLM">
        <activity android:name=".Login"></activity>
        <activity android:name=".SignUp" />
        <activity android:name=".video.ShowVideo" />
        <activity android:name=".FullImageView" />
        <activity android:name=".pdf.PdfViewerActivity" />
        <activity
            android:name=".pdf.PdfActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>